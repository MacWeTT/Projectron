import React from "react";
import Navbar from "../Components/Navbar";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Home;
